# @astro-slides/cli

The `astro-slides` command-line tool.

Lazy-loaded subcommands (citty) for `dev`, `build`, `export`, and `mcp-server`.
The `astro-slides` bin is declared once there's a built entry point to point at.

Status: skeleton (Phase 01).
