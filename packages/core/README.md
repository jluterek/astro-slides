# @astro-slides/core

The Astro integration that turns a project into a deck.

Provides the Astro integration, Vite plugins, virtual modules, content-collection
wiring, and per-slide routes. Bundles a fallback `starter` theme under
`src/themes/starter/`.

Status: skeleton (Phase 01). Implemented in Phase 03.
