{
  "extends": "../../tsconfig.base.json",
  "//": "Browser runtime bundled by Vite/Astro — bundler resolution matches how it ships and lets `exports`-map-only deps (tinykeys) resolve their types.",
  "compilerOptions": {
    "composite": true,
    "rootDir": "src",
    "outDir": "dist",
    "module": "esnext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src"],
  "references": [{ "path": "../types" }]
}
