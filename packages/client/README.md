# @astro-slides/client

The in-browser runtime.

Slide rendering, navigation and URL state, keyboard/touch input, the
`past/present/future` state machine, click runtime, slide transitions, and
presenter mode UI.

Status: skeleton (Phase 01). Implemented across Phases 04–11.
