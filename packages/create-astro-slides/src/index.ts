// create-astro-slides — programmatic surface (the runnable entry is `bin/create-astro-slides.mjs`).
export {
  isDirEmpty,
  packageName,
  run,
  type ScaffoldOptions,
  scaffold,
  type ThemeChoice,
  titleFromName,
  writeScaffold,
} from "./main.js";
